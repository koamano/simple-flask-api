apiVersion: v1
kind: Service
metadata:
    name: "akskoamano-0664"
    labels:
        app: "akskoamano-0664"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akskoamano-0664"